
/*

Mpp <key,val>

generally we store value as count default
or index if we want
mpp[s[i]] = i

mpp.find(elem) != mpp.end():
Checks if the element elem exists in the map.
Returns true if found, false if not.
  
mpp.find(elem) == mpp.end():
Checks if the element elem does not exist in the map.
Returns true if not found, false if found.
  */
